#include<stdio.h>
#include<conio.h>
void main();

int a[50],n,i,max,min;
clrscr();
printf("enter number of elements");
scanf("%d",&n);
for(i=0;i<n;i++)
}
scanf("%d",&a[i]);
}max = min=a[0];
for(i=1;i<n;i++)
}
if(a[i]>max)
max=a[i];
if(a[i]<min)
min=a[i]
}
printf{"maximum=%d",max);
printf("\nminimum=%d",min);
getch();
}
